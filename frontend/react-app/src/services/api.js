/**
 * @deprecated Ce fichier est conservé pour compatibilité ascendante.
 * Utiliser directement `services/apiClient` pour les nouvelles fonctionnalités.
 * Les helpers login/logout sont dans `services/authService`.
 */
export { default } from "./apiClient";
