"use strict";

/**
 * space-schedule controller
 */

const { createCoreController } = require("@strapi/strapi").factories;

module.exports = createCoreController("api::space-schedule.space-schedule");
